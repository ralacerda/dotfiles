- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zsh: .zsh
    ~/.zshrc:
       path: .zshrc
       force: true
    ~/.gitconfig:
       path: .gitconfig
       force: true
    ~/.local/share/konsole: 
       force: true
       path: konsole

- shell:
  - [git submodule update --init --recursive, Installing submodules]
